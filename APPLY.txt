MRAPI DEV v0.3.4-alpha.2 — WINDOWS CODEX CMD FIX

OBJECTIVE
Fix Shadow automatic Codex execution on Windows.

CAUSE
`where codex` succeeds because the global npm install exposes codex.ps1,
but spawning bare `codex` from Node fails with ENOENT / PowerShell policy issues.
Shadow already verified `codex.cmd --version` works.

IMPLEMENTATION
Prefer `codex.cmd` first on Windows, then fall back to bare `codex`.

TESTS
node --test test/codex-windows-command.test.js
node --test

SUCCESS
All tests pass.

STOP
Do not deploy.
No Cloud Run changes.
