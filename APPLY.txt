OVERLAY v0.3.3-alpha.0

Copy this overlay over the CURRENT repository.

Then ask Codex to run:

node --test test/codex-desktop-handoff.test.js
node --test

Do not deploy from Codex.

Expected:
- full Brain output is passed into the generated Codex prompt;
- prompt is written to a local file;
- prompt is copied to Windows clipboard;
- no hardcoded ChatGPT desktop app path;
- existing tests remain passing.
